#in aws portal i have launched amazon linux 
  #you need to login inside the aws instance upload the public key from the master node 
  sudo -i
  ###then add the public key 
  ###go back to your master machine eastibilish the ssh connection ssh root@private ip
  ##inside the project folder in host.ini file in web block add the inventory 
  ansible all -m ping
  ##3 it should ping your amazon node also
