##inside the ansible master node we will genrate teh ssh key 
sudo -i
  ssh-keygen -t rsa -b 4096
  #####just press enter enter this will generate the public private key you can get the pub key 
  36  cat /root/.ssh/id_rsa.pub
  #####you need to login inside your worker node once you login inside your worker node we need to paste the public key in authorized_key
  sudo -i
  ##open the file using nano editor go to the end of the line press enter and paste the public key at the end
  15  nano /root/.ssh/authorized_keys
  ##save the file using ctrl x press y and enter

  ##finally once you saved the file 
  #####copy the private ip address of the instance
  ##go to the master machine terminal 
  ssh root@privateip
  ###say yes and you will be login to the particular machine 
  exit
#################################
  42  cd /root/project/
   43  ls
   44  rm ansible.cfg
   45  vi ansible.cfg
#inside ansible.cfg add this values
[defaults]
inventory =  host.ini
host_key_checking=false
remote_user=root
log_path = ./log/ansible.log
forks = 5

   46  mkdir log
##inside that add the worker node details
   49  vi host.ini (do not forget to change web1 ip to your worker node ip address
[ansible]
ansible-master ansible_host=127.0.0.1
[web]
web1 ansible_host=172.31.82.114

   50  ansible all -m ping
   51  ansible web1 -m ping
#in master node ansible try to do ssh in 127.0.0.1
ansible-master | UNREACHABLE! => {
    "changed": false,
    "msg": "Failed to connect to the host via ssh: root@127.0.0.1: Permission denied (publickey,password).",
    "unreachable": true
}
##to resolve the error we can do inside the inventory file change the master node inventory to this 
vi host.ini
[ansible]
ansible-master ansible_connection=local
[web]
web1 ansible_host=172.31.82.114
##then run the command 
ansible all -m ping 
#####################################
#this is for refrence purpose like a common patch is deployed for dev and test environment this way i can apply the same
 69  ansible -i inventory/dev.ini -i inventory/prod.ini -m ping
#inside the inventory folder if we have test.ini dev.ini prod.ini and i want to execute all the inventory 
   70  ansible -i inventory all -m ping



